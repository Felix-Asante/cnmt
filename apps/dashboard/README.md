# Dashboard

Internal operations app for C.N Connect. Vite + TanStack Router, shared Tailwind tokens from `@repo/tailwind-config`.

```bash
bun --filter dashboard dev
```

Runs on [http://localhost:3001](http://localhost:3001).
